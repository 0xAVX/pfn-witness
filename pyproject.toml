[project]
name = "pfn-witness"
version = "0.1.0"
description = "PFN Witness: decoder provenance, audit and repair for TabPFN-3.5"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
    "tabpfn==9.0.0",
    "tabpfn-extensions==0.6.2",
    "pandas",
    "numpy",
    "scikit-learn",
    "torch",
    "flask",
    "matplotlib",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
